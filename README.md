# Relay Modern - Instagram Example

This was project was created with `create-react-app` and intends to demonstrate a minimal example for Relay Modern.

## Install & Run

```sh
yarn install
yarn start
```

